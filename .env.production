# Vite loads this file ONLY for production builds (`vite build` / `npm run build`),
# never for `vite` dev server. Local dev keeps falling through to the
# `http://localhost:4000` fallback inside src/app/api/client.ts.
#
# Set to the public URL of the hosted Spring Boot API.
VITE_API_BASE=http://165.232.164.4:8081
