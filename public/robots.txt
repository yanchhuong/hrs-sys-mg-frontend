User-agent: *
Allow: /

# Point crawlers straight at the static sitemap so /km + /zh + /cambodia
# get indexed on the first crawl instead of after link-graph
# discovery. Update the hostname if the deploy target changes.
Sitemap: https://smart-hrms.com/sitemap.xml

# v-shop-sitemap — additional dynamic sitemap enumerating every
# enabled public shop. Served by the API (droplet) — a tenant's
# shop is discoverable by name once Google fetches this list.
Sitemap: https://hr-share.com/api/v1/public/shop/sitemap.xml
